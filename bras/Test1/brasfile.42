puts "msg: warning from Newer-Rule `dependency not built'"

puts "expect: `' -> `xxxx'"
puts "expect: bras(warning): dependency `yyyy' of target xxxx was not made"



Newer xxxx yyyy {
  puts "`$trigger' -> `$target'"
}

Exist yyyy {} {
  ## The target is not made so the warning is triggered
}
