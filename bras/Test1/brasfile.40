puts "msg: SearchPath, make bla.o in build from from ../unix/bla.c"
puts "expect: cd /home1/kir/work/bras/Test1/build"
puts "expect: making bla.o from ../unix/bla.c"
puts "expect: cd /home1/kir/work/bras/Test1"
puts "expect: rm -rf build unix"

exec rm -rf build unix
exec mkdir -p build unix
exec touch unix/bla.c

PatternNewer .*\.o .c {
  puts "making $target from $trigger"
}

## This rule forwards us to subdirectory build. The rules to obey
## therein are declared below.
Always all @build/all {
  rm -rf build unix
}

## Change to the build-directory so that the relevant rules apply to
## that directory.
cd build

searchpath {. ../unix}

Always all bla.o {
}

## Back to main directory (not really necessary)
cd ..

